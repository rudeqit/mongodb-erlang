%% ex: ts=4 sw=4 noexpandtab syntax=erlang
{application, mongodberl, [
  {description, "Client interface to MongoDB, also known as the driver. See www.mongodb.org"},
  {vsn, "3.0.0"},
  {registered, []},
  {applications, [kernel, stdlib, bson, crypto, poolboy]},
  {mod, {mongo_app, []}}
]}.
